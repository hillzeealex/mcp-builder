{
  "name": "@mcp-builder/core",
  "version": "0.1.0",
  "description": "Pure, deterministic generator that turns a typed MCP server definition into project files.",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "jiti": "^2.4.2",
    "prettier": "^3.4.2",
    "yaml": "^2.6.1",
    "zod": "^3.25.76"
  }
}
